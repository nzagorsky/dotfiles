- aptget:
  - ack-grep
  - docker
  - docker-compose
  - htop
  - python3-pip
  - tmux
  - zsh
  - software-properties-common

- shell:
  - sudo add-apt-repository ppa:neovim-ppa/stable
  - sudo apt update
  - sudo apt install neovim

