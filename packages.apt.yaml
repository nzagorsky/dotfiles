- script:
  - [[ `whoami` == "root" ]] && apt update && apt install sudo
  - apt install python3-apt

- aptget:
  - ack-grep
  - docker
  - docker-compose
  - htop
  - python3-pip
  - tmux
  - zsh

- aptget:
    nvim:
      ppa_source: ppa:neovim-ppa/unstable
